[project]
name = "lead-crawler"
version = "0.1.0"
description = "Crawler for pulling tiktok emails."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.9.5",
    "python-dotenv>=1.0.0",
    "agentmail>=0.1.0",
    "TikTokApi>=6.0.0",
    "playwright>=1.55.0",
    "supabase>=2.0.0",
    "openai>=1.0.0",
]
